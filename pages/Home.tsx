import React from 'react';
import Hero from '../components/Hero';

const Home: React.FC = () => {
  return (
    <div>
      <Hero />
      {/* Optional: Add teaser sections or keeping it clean as just Hero per structure update */}
    </div>
  );
};

export default Home;